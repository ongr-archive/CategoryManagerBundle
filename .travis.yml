language: php
php:
  - 5.5
env:
  - SYMFONY="2.5.*"
services:
  - elasticsearch
install:
  - composer require --no-update symfony/symfony:${SYMFONY}
before_script:
  - echo "USE mysql;\nUPDATE user SET password=PASSWORD('root') WHERE user='root';\nFLUSH PRIVILEGES;\n" | mysql -u root
  - export COMPOSER_CACHE_DIR=$TRAVIS_BUILD_DIR/composercache; /usr/bin/hhvm /home/travis/.phpenv/versions/`phpenv version | awk '{print $1}'`/bin/composer update --prefer-dist -vvv --profile
script:
  - vendor/bin/phpunit --coverage-text
  - vendor/bin/phpcs -p --standard=$TRAVIS_BUILD_DIR/vendor/ongr/ongr-strict-standard/ONGR --ignore=vendor/,Tests/app/ --extensions=php  ./
cache:
  directories:
    - vendor
    - composercache
